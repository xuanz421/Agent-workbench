# Codex Adapter

Keep Codex-specific integration instructions here. The canonical workflow content remains under `/skills`, `/agents`, and `/rules`.

Prefer symlinks or a thin installation script instead of duplicating skill contents.
