---
name: refactoring
description: Refactor existing code without changing intended behavior unless the task explicitly requires behavioral change.
---

# refactoring

Establish current behavior, define the structural problem, preserve interfaces where possible, make incremental changes, and validate behavior after each meaningful step. Avoid abstraction for its own sake.
